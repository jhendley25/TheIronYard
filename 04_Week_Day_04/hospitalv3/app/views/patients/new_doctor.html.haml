%h3 Add Doctor

= form_for [@patient,@create_doctor], url: create_doctor_institution_patient_path(@institution,@patient) do |f|
  - if @patient.errors.any?
    #error_explanation
      %h2= "#{pluralize(@patient.errors.count, "error")} prohibit this doctor from being saved:"
      %ul
        -@doctor.errors.full_messages do |msg|
          %li = msg
   
  = f.label :name
  = f.text_area :name

  = f.submit 'Save'